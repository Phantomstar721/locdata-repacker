[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "locdata-repacker"
version = "1.0.0"
description = "Unpack and repack Fantasy Wars locdata.md localization files"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["src"]

